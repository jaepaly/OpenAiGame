{
  "name": "cloud-harvest-inc",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build && node scripts/prepare-sites-dist.mjs",
    "build:pages": "tsc && vite build --mode pages",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "@openai/sites-vite-plugin": "0.2.0",
    "typescript": "^5.9.2",
    "vite": "^8.0.0"
  }
}
